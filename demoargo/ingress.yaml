apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-ingress
  namespace: demoargo
spec:
  rules:
  - host: 192.168.49.2
    http:
      paths:
      - path: /truc
        pathType: Prefix
        backend:
          service:
            name: demo-service
            port:
              number: 80
  # defaultBackend:
  #   service:
  #     name: demo-service
  #     port:
  #       number: 80